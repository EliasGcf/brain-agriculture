# Server

Backend context for the agricultural system. Shared domain vocabulary and rules live in the root [CONTEXT.md](../../CONTEXT.md); this context adds no server-specific domain terms at present.

## Language

The server uses the shared agricultural domain language without redefining it locally.
